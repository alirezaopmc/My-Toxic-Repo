#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

#define l(i,s,n) for(int i=s; i<n; i++)
#define ll long long

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
    
    int n;
    cin >> n;
    
}